#ifndef EXEC_H
#define EXEC_H

// We shall assume standard input to be the card reader
// Thus, to get started, one must pipe a loading routine
// card into standard input
void go_button(Mix* machine);

#endif